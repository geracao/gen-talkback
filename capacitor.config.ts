import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'GEN TalkBack',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
